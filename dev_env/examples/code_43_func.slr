bmi = weight / (height * height);

if (bmi > 25)
{
  overweight = 1;
  print("You ", "are " ,  "overweight!", " because BMI is over 25." , num_to_str( bmi ) ,  "END")
} else {
  overweight = 0;
  print("You ", "are " , "not " , "overweight!", " because BMI is smaller than or equal to 25.\n")
}

greeting = "KONNICHIWA";
print( greeting );
