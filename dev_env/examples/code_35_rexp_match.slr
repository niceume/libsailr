first = "Dr."
second = "Ken"

fullname = first + "-" + second 

pattern = re/^Dr\.(.*)/

if (fullname =~ pattern ){
	great_dr = "Yes";
} else {
	great_dr = "No";
}

match = rexp_matched(1)

print( match );
